// Model types only. Prisma exposes plain model types here suffixed with
// `Model` (e.g. `UserModel`) to avoid clashing with its internal type names.
//
// `export type *` guarantees nothing survives into the JavaScript output, so
// importing this from browser code costs zero bytes.
export type * from "./generated/prisma/models";
